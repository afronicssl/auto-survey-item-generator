# Automatic Item Generator (Streamlit App)

This app automatically generates coded questionnaire items.

## Features
- Input multiple constructs
- Auto-generate item codes (e.g., Marketing Strategy → MS1, MS2, …)
- Streamlit-based web interface

## How to Run
```bash
pip install -r requirements.txt
streamlit run app.py
```
